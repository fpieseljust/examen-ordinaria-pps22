const getHashedPassword = (password) => {
}

const checkPassword = (password, hash) => {
}

const getJWT = (user) => {
}

function verifyJWT(req, res, next){
};

const encrypt = (text) => {
};

const decrypt = (hash) => {
};

module.exports = { getHashedPassword, checkPassword, getJWT, verifyJWT, encrypt, decrypt}